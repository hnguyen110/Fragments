version: "3.9"
services:
  api:
    image: ${DOCKER_REPO}:${DOCKER_TAG}
    init: true
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - AWS_COGNITO_POOL_ID
      - AWS_COGNITO_CLIENT_ID